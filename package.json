{
  "name": "grimorio-dnd",
  "version": "1.0.0",
  "private": true,
  "description": "Biblioteca de conjuros de D&D (reglas 2024) en español, filtrable por clase y nivel.",
  "scripts": {
    "dev": "next dev --webpack",
    "build": "next build --webpack",
    "start": "next start",
    "lint": "next lint",
    "srd:extract": "node scripts/extract-srd.mjs && node scripts/slice-srd.mjs",
    "srd:build": "node scripts/assemble-classes.mjs && node scripts/validate-srd.mjs"
  },
  "dependencies": {
    "next": "^16.0.10",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  }
}
